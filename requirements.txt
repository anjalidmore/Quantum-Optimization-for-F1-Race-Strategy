# Quantum Optimization for Formula 1 Race Strategy
# Unified dependency list for the whole platform (Tasks 1-6+).
# Install with: pip install -r requirements.txt

# --- Core (Tasks 1-4: knowledge representation, expert system, search, data) ---
networkx>=3.0
rdflib>=7.0
owlready2>=0.46
matplotlib>=3.7
pandas>=2.0
numpy>=1.24
scipy>=1.10
seaborn>=0.12
tabulate>=0.9

# --- Machine Learning (Task 6) ---
scikit-learn>=1.3
joblib>=1.3
# Optional. If the native libomp runtime is unavailable, the ML pipeline
# detects this at import time and reports "XGBoost unavailable — skipped"
# instead of failing or fabricating results.
xgboost>=2.0

# --- Backend API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Testing & linting ---
pytest>=7.4
httpx>=0.27
ruff>=0.6

# --- Real data (Task 5/6 on genuine FastF1 telemetry instead of the
#     synthetic demo) ---
fastf1>=3.5
# Needed only to programmatically execute docs/notebooks/task5_feature_engineering.ipynb
# after fetching real data (see scripts/fetch_real_session.py). Not needed if
# you re-run the notebook by hand in Jupyter/VS Code instead.
nbclient>=0.10
nbformat>=5.10
ipykernel>=6.29
