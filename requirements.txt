# Quantum Optimization for Formula 1 Race Strategy
# Unified dependency list for the whole platform (Tasks 1-6+).
# Install with: pip install -r requirements.txt

# --- Core (Tasks 1-4: knowledge representation, expert system, search, data) ---
networkx>=3.0
rdflib>=7.0
owlready2>=0.46
matplotlib>=3.7
pandas>=2.0
numpy>=1.24
scipy>=1.10
seaborn>=0.12
tabulate>=0.9

# --- Machine Learning (Task 6) ---
scikit-learn>=1.3
joblib>=1.3
# Optional. If the native libomp runtime is unavailable, the ML pipeline
# detects this at import time and reports "XGBoost unavailable — skipped"
# instead of failing or fabricating results.
xgboost>=2.0

# --- Deep Learning (Task 7) ---
# Keras 3 is backend-agnostic. TensorFlow publishes NO wheel for Python 3.14
# (verified: `pip index versions tensorflow` returns no distribution), so the
# PyTorch backend is used and app/core/runtime.py sets KERAS_BACKEND=torch.
# The Keras API in app/intelligence/dl/ is identical on either backend.
#
# Import-order note: torch ships its own libomp, and XGBoost segfaults on macOS
# if it initialises against PyTorch's copy. app/core/runtime.py imports XGBoost
# first to claim the OpenMP runtime - always go through prepare_dl_runtime()
# rather than importing keras or torch directly.
keras>=3.5
torch>=2.4

# --- Explainable AI (Task 8) ---
shap>=0.44          # Shapley attributions (TreeExplainer exact, KernelExplainer model-agnostic)
lime>=0.2           # local linear surrogates, for comparison against SHAP
dice-ml>=0.11       # diverse counterfactuals (time-bounded; see xai/counterfactual.py)

# --- Backend API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Testing & linting ---
pytest>=7.4
httpx>=0.27
ruff>=0.6

# --- Real data (Task 5/6 on genuine FastF1 telemetry instead of the
#     synthetic demo) ---
fastf1>=3.5
# Needed only to programmatically execute docs/notebooks/task5_feature_engineering.ipynb
# after fetching real data (see scripts/fetch_real_session.py). Not needed if
# you re-run the notebook by hand in Jupyter/VS Code instead.
nbclient>=0.10
nbformat>=5.10
ipykernel>=6.29
